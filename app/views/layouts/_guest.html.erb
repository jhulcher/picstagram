<form type="hidden" action="<%= session_url %>" method="post">
  <%= auth_token_input %>
  <input type="hidden"
         name="user[username]"
         value="don">
  <input type="hidden"
         name="user[password]"
         value="12345678">
Or sign in as a <input type="submit" value="guest!" class="guest under">
