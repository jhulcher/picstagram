<form type="hidden" action="<%= session_url %>" method="post">
  <%= auth_token_input %>
  <input type="hidden"
         name="user[username]"
         value="don">
  <input type="hidden"
         name="user[password]"
         value="12345678">
  <div class="inline">
    <div class="average-text headerleft guesttext">
      Or sign in as a
    </div>
    <div class="right linktosignin">
      <button value="guest!" class="guest">Guest</button>
    </div>
  </div>
</form>
